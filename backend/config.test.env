# 测试环境配置
PORT=3001
NODE_ENV=test
JWT_SECRET=shopgenius_jwt_secret_key_for_test
JWT_EXPIRE=1d

# 数据库配置 - 测试环境（使用与开发环境相同的配置）
DB_HOST=localhost
DB_PORT=3306
DB_NAME=shopgenius
DB_USER=root
DB_PASSWORD=admin

# AI服务配置（使用模拟数据）
OPENAI_API_KEY=mock_key

# 日志配置
LOG_LEVEL=info
